import { style } from '@vanilla-extract/css';

export const productImage = style({
  width: '3rem',
  height: 'auto',
});

export const mapMarker = style({ marginInlineStart: '0.25rem' });

export const delivery = style({ marginInlineStart: '0.5rem' });
